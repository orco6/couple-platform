# src/brand — identity

- `brand.ts` — product name, legal identity for printed documents, locale (language, direction, timezone, currency)
- `theme.css` — raw colour/shape values (keep the contrast notes true)
- `Logo.tsx` — mark + wordmark
- Also: `src/app/icon.svg` (favicon) and the font import in `src/app/layout.tsx`

See DESIGN_REVIEW.md → "Giving a business its identity".
